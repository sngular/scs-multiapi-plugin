asyncapi: '2.5.0'
info:
  title: Test API
  version: '1.0.0'
  description: Some test API
servers:
  default:
    url: mqtt://localhost
    protocol: mqtt
channels:
  test:
    publish:
      summary: This is just a test.
      operationId: onTest
      message:
        name: TestMsg
        payload:
          type: object
          properties:
            id:
              type: integer
